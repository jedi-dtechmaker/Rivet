# PostgreSQL Database URL (e.g., from Neon, Supabase, or local Docker)
DATABASE_URL="postgresql://user:password@localhost:5432/rivet"

# NextAuth Secret (Run `openssl rand -base64 32` to generate one)
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# GitHub OAuth Credentials (Get these from GitHub Developer Settings)
GITHUB_ID=""
GITHUB_SECRET=""
